{
  "name": "soundous-companion",
  "version": "1.0.0",
  "description": "A personal AI companion",
  "scripts": {
    "build": "mkdir dist && cp -r components services utils App.tsx index.html index.tsx metadata.json pages.tsx types.ts dist"
  },
  "dependencies": {
    "@google/genai": "^1.12.0",
    "@netlify/functions": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}