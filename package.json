{
  "name": "soundous-companion",
  "version": "1.0.0",
  "description": "A personal AI companion",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.html dist/ && esbuild index.tsx --bundle --outfile=dist/index.js --platform=browser --define:process.env.NODE_ENV='\"production\"'"
  },
  "dependencies": {
    "@google/genai": "^1.12.0",
    "@netlify/functions": "^2.7.0",
    "lucide-react": "^0.309.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.2",
    "typescript": "^5.0.0"
  }
}
